# humanify — provenance

The idea and content of this skill were taken from the **humanizer** Claude Code skill by Siqi Chen (`@blader`).

- Original repository: https://github.com/blader/humanizer
- Original author: Siqi Chen (https://github.com/blader)
- License: MIT (see `LICENSE`)

## Changes from upstream

- Renamed the skill from `humanizer` to `humanify` (frontmatter `name` field) so it fits this plugin's naming.
- Repackaged into the `communication` plugin within the `jorgeiglopez-toolkit` marketplace.

The body of `SKILL.md` is otherwise the upstream content. If you make further edits, note them here so the divergence from upstream stays visible.
