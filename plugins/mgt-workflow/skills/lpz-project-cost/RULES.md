---
name: lpz-project-cost
lastUpdate: 2026-07-03 00:00
---

# Rules
- Run the bundled `estimate_cost.py` script and relay its output verbatim — the script output is the answer, add no commentary.
- Let it auto-detect the project's transcript dir from `$PWD`; pass an explicit path only if auto-detection fails.
