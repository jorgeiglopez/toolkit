# Dispatch constraints

Standing rules every dispatched agent in this project must obey. Copy this
file to `<project>/.claude/lpz-agent-dispatch/dispatch-constraints.md` and fill in
the sections below. Delete any that don't apply; add project-specific ones.

## Environment

- OS / runtime versions:
- Package manager (and the one command to install deps):
- How to run the app locally:

## Tooling

- Test command (the project gate):
- Lint / format / type-check commands:
- Tools or commands that are BANNED here:

## Boundaries

- Directories or files agents must not touch:
- Secrets / config that never get committed:
- Timeouts and resource limits (ports, DB names, temp dirs):

## Conventions

- Commit / branch rules:
- Anything an agent would otherwise get wrong on this project:
